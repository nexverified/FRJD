# Set only verified customer-facing destinations. Blank values hide direct links.
FRJD_CONTACT_EMAIL=
FRJD_WHATSAPP_NUMBER=
# Canonical public origin; change when the production domain is connected.
FRJD_SITE_ORIGIN=https://frjd-sourcing.bhuvanraju66.chatgpt.site
# Strong random server-side secret (at least 32 characters) for the Worker-only
# operator console and APIs. Without it, operator access fails closed.
FRJD_ADMIN_TOKEN=
# Local preview port. Hosted deployments use the Worker fetch handler.
PORT=4173

